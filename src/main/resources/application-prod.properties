#spring.datasource.url=jdbc:postgresql://localhost:5432/mealplanner
#spring.datasource.username=postgres
#spring.datasource.password=postgres
#spring.datasource.driver-class-name=org.postgresql.Driver
#spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
##spring.jpa.hibernate.ddl-auto=update
##spring.jpa.hibernate.ddl-auto=validate
## To allow Flyway to run once, do this:
##spring.jpa.hibernate.ddl-auto=none
#spring.jpa.hibernate.ddl-auto=create
## FIX Hibernate validation is running BEFORE Flyway migrations.
#spring.jpa.defer-datasource-initialization=false
#
#spring.flyway.enabled=true
## FIX Hibernate validation is running BEFORE Flyway migrations.
#spring.flyway.locations=classpath:db/migration
## If you still get Unsupported Database, you can tell Flyway to skip the DB version check:
#spring.flyway.validate-on-migrate=false
## If your table is in public, force Flyway to use it:
#spring.flyway.schemas=public
## And (optional but safe):
#spring.jpa.properties.hibernate.default_schema=public
## baseline-on-migrate=true tells Flyway:
##?This DB already exists ? start tracking from now?
##spring.flyway.baseline-on-migrate=true
##baseline-version=1 means:
##?Treat current DB state as V1?
##DO NOT create V1__*.sql
##spring.flyway.baseline-version=1
##spring.flyway.locations=classpath:db/migration
#
##ddl-auto=validate or none # prod Safety
## FORCE Flyway to log (this is key)
#logging.level.org.flywaydb=DEBUG

spring.datasource.url=jdbc:postgresql://localhost:5432/mealplanner
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=validate
# no circular dependency error
#spring.jpa.defer-datasource-initialization=false
spring.jpa.defer-datasource-initialization=true

spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true
spring.flyway.locations=classpath:db/migration
spring.flyway.validate-on-migrate=false